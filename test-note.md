# Тестовая заметка Obsidian

Это тестовая заметка для проверки всех возможностей парсинга Markdown в стиле Obsidian.

## Основное форматирование

Обычный текст с **жирным шрифтом**, *курсивом*, ***жирным курсивом***.

Можно использовать ==выделение текста маркером==.

~~Зачеркнутый текст~~

> Обычная цитата без callout
> Продолжение цитаты

## WikiLinks (внутренние ссылки)

Простая ссылка на файл в той же папке: [[VSA]]

Ссылка с алиасом: [[VSA|Виртуальный Анализ Спреда]]

Ссылка на файл в подпапке: [[Architecture/Component]]

Относительная ссылка: [[../Other/File]]

Ссылка с расширением: [[VSA.md]]

## Callouts (блоки внимания)

> [!note] Обычная заметка
> Это простая заметка для информации.
> Может содержать несколько строк.

> [!tip] Совет
> Используйте горячие клавиши для ускорения работы!

> [!info] Информация
> `bcrypt.DefaultCost` равен **10**. 
> Это "золотая середина": время хэширования ~100-200 мс.

> [!warning] Предупреждение
> Будьте осторожны при работе с production базой данных!

> [!danger] Опасность
> Никогда не вставляйте переменные в строку через `fmt.Sprintf`! 
> Используйте `?` (**плейсхолдеры**).

> [!success] Успех
> Тесты пройдены успешно! ✅

> [!question] Вопрос
> Как лучше оптимизировать этот запрос?

> [!example] Пример кода
> ```go
> func main() {
>     fmt.Println("Hello, World!")
> }
> ```

> [!quote] Цитата
> "Простота — высшая степень изощрённости"
> — Леонардо да Винчи

## Callout с кодом внутри

> [!tip] Анонимная функция с defer
> ```go
> defer func() {
>     if r := recover(); r != nil {
>         log.Printf("Recovered: %v", r)
>     }
> }()
> ```
> Этот паттерн часто используется для обработки паники.

## Списки

### Маркированный список

- Первый пункт
- Второй пункт
  - Вложенный пункт
  - Еще один вложенный
    - Глубокая вложенность
- Третий пункт

### Нумерованный список

1. Первый шаг
2. Второй шаг
   3. Подшаг 2.1
   4. Подшаг 2.2
5. Третий шаг

### Чек-листы (Task lists)

- [x] Завершенная задача
- [x] Еще одна выполненная
- [ ] Невыполненная задача
- [ ] Тоже ждет выполнения
  - [x] Вложенная завершенная
  - [ ] Вложенная незавершенная

## Код

### Inline код

Используйте команду `npm install` для установки зависимостей.

Переменная `userName` содержит имя пользователя.

### Блоки кода

JavaScript:
```javascript
const greeting = (name) => {
  return `Hello, ${name}!`;
};

console.log(greeting('World'));
```

TypeScript:
```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

const user: User = {
  id: 1,
  name: 'John Doe',
  email: 'john@example.com'
};
```

Python:
```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

print(fibonacci(10))
```

Go:
```go
package main

import (
    "fmt"
    "time"
)

func main() {
    ticker := time.NewTicker(1 * time.Second)
    defer ticker.Stop()
    
    for range ticker.C {
        fmt.Println("Tick")
    }
}
```

SQL:
```sql
SELECT 
    u.id,
    u.name,
    COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.created_at > '2024-01-01'
GROUP BY u.id, u.name
ORDER BY order_count DESC
LIMIT 10;
```

Bash:
```bash
#!/bin/bash

for file in *.txt; do
    echo "Processing $file"
    cat "$file" | grep "error" > "${file}.errors"
done
```

## Таблицы

| Язык       | Типизация | Год создания | Популярность |
|------------|-----------|--------------|--------------|
| JavaScript | Динамическая | 1995      | ⭐⭐⭐⭐⭐ |
| TypeScript | Статическая  | 2012      | ⭐⭐⭐⭐   |
| Python     | Динамическая | 1991      | ⭐⭐⭐⭐⭐ |
| Go         | Статическая  | 2009      | ⭐⭐⭐⭐   |
| Rust       | Статическая  | 2010      | ⭐⭐⭐     |

### Таблица с выравниванием

| Слева | Центр | Справа |
|:------|:-----:|-------:|
| Text  | Text  | Text   |
| A     | B     | C      |
| 1     | 2     | 3      |

## Переносы строк

Первая строка
Вторая строка (с переносом благодаря remark-breaks)

Без пустой строки между ними они должны быть на разных строках.

А это новый параграф после пустой строки.

## Горизонтальная линия

Текст до линии

---

Текст после линии

## Комбинированный пример

> [!danger] SQL Инъекции
> **Никогда** не делайте так:
> 
> ```go
> // ❌ ПЛОХО - уязвимо к SQL injection
> query := fmt.Sprintf("SELECT * FROM users WHERE id = %s", userInput)
> db.Query(query)
> ```
> 
> Вместо этого используйте плейсхолдеры:
> 
> ```go
> // ✅ ХОРОШО - безопасно
> query := "SELECT * FROM users WHERE id = ?"
> db.Query(query, userInput)
> ```
> 
> Подробнее: [[Security/SQL-Injection-Prevention]]

## Вложенные структуры

1. Первый уровень
   - Маркер внутри нумерованного
   - Еще один маркер
     1. Нумерация третьего уровня
     2. Продолжение
   - Возврат на второй уровень
2. Продолжение первого уровня
   
   > [!tip] Callout внутри списка
   > Это работает!
   
3. Третий пункт

## Ссылки и изображения

Обычная ссылка: [Google](https://google.com)

Ссылка с title: [GitHub](https://github.com "Перейти на GitHub")

Автоссылка: https://example.com

Email: example@email.com

## Эмодзи и Unicode

🚀 Ракета
💡 Лампочка  
✅ Галочка
❌ Крестик
⚠️ Предупреждение
🔥 Огонь
📝 Заметка
🎯 Цель

## Экранирование

\*Это не курсив\*

\[\[Это не ссылка\]\]

\> Это не цитата

\`Это не код\`

## Финальный тест

> [!example] Комплексный пример
> 
> ### Архитектура приложения
> 
> Наше приложение состоит из нескольких компонентов:
> 
> 1. **Frontend** - [[Architecture/Frontend]]
>    - Vue 3 + TypeScript
>    - Tailwind CSS
>    - Pinia для стейта
> 
> 2. **Backend** - [[Architecture/Backend]]
>    ```typescript
>    interface APIResponse {
>      data: unknown;
>      error?: string;
>    }
>    ```
> 
> 3. **Database** - PostgreSQL
>    - [x] Миграции настроены
>    - [x] Индексы созданы
>    - [ ] Бэкапы автоматизированы
> 
> | Компонент | Статус | Версия |
> |-----------|--------|--------|
> | Frontend  | ✅     | 1.0.0  |
> | Backend   | ✅     | 1.0.0  |
> | Database  | ⚠️     | 14.5   |

---

**Итог:** Эта заметка покрывает все основные возможности Obsidian-стайл Markdown!