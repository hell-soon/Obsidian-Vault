- Для отката локального commit 
```shell
git restet HEAD~
```
- Для отката послденего *залитого* зименения 
```bash
git reset HEAD~1 // Удаляем 1 последний коммит

git push -f // и пушим
```

- oh my zsh (Git Plugins) -- alias
```shell
ga='git add'
gaa='git add --all'

gc='git commit'
gcam='git commit --all --message'

gb='git branch'

gcd='git checkout $(git_develop_branch)'
gcm='git checkout $(git_main_branch)'

gstall='git stash --all'
gstaa='git stash apply'
gstc='git stash clear'
gstd='git stash drop'
gstl='git stash list'
gstp='git stash pop'

gp='git push'

gl='git pull'

grh HEAD~1='git reset'
```
